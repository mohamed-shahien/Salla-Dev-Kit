# Component Blueprint

استخدم هذا القالب قبل تنفيذ أي مكون جديد أو كبير.

## بيانات المكون

- اسم المكون العربي:
- اسم المكون الإنجليزي:
- اسم ملف Twig:
- كلاس الجذر:
- هل تم إنشاؤه من Salla Partners:
- `key` من Salla Partners:
- `path` من Salla Partners:
- `is_default`:
- `icon`:

## الغرض

ما المشكلة أو التجربة التي يقدمها المكون للتاجر أو العميل؟

## إعدادات عامة مطلوبة

اكتب هنا أي إعداد يجب أن يكون في `settings` لأنه مشترك بين أكثر من مكون:

```text
- لا يوجد
```

## Fields خاصة بالمكون

اكتب الحقول المطلوبة:

| id | type | format | default | description |
|----|------|--------|---------|-------------|
| title | text | - | - | عنوان القسم |

## Twig

- هل يبدأ بـ `{% set c = component %}`:
- هل يستخدم helpers:
- هل توجد شروط empty state:
- هل توجد fallbacks:

## CSS

- الملف:
- تعليق المكون:
- المتغيرات المستخدمة:

## JS

- هل يحتاج JS:
- ملف الخدمة:
- selector:
- key:
- طريقة init:

## تحقق قبل التسليم

- لا يوجد `script` داخل Twig.
- لا يوجد `style` داخل Twig.
- لا توجد متغيرات Capital في Twig.
- JS محمل شرطيا.
- CSS لا يكسر صفحات أخرى.
